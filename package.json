{
  "name": "lb-vue-monorepo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "packageManager": "pnpm@10.12.1",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "scripts": {
    "install": "pnpm i -r ",
    "ui": "pnpm -F @lb-vue-monorepo/ui",
    "editor": "pnpm -F @lb-vue-monorepo/editor",
    "dev": "pnpm editor dev",
    "build": "pnpm build:ui && build:editor",
    "build:editor": "pnpm editor build",
    "build:ui": "pnpm ui build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clear": "rimraf -g ./node_modules ./packages/**/node_modules ./packages/**/dist",
    "prepare": "husky"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1",
    "husky": "^9.1.7",
    "rimraf": "^6.0.1"
  }
}